//	ehmenor= (ano1 < ano2) ||
			//((ano1==ano2) && (mes1 < mes2)) ||
			 //((ano1==ano2) && (mes1 == mes2));	
		
public class maior_data {
	public static void main(String[]args){
		
	String nome1, nome2, nome3;
	int dia1, mes1, ano1, dia2, mes2, ano2, dia3, mes3, ano3;

		diam=dia1;
		mesm=mes1;
		anom=ano1;
		
	if((ano2 <anom) || ((ano2== anom) && (mes2 < mesm)) ||
	  ((ano2==anom)&& (mes2==mesm) && (dia2<diam))))
	  { diam = dia2;
		mesm = mes2;
		anom = ano2;
	}	
	if((ano3 <anom) || ((ano3== anom) && (mes3 < mesm)) ||
	  ((ano3==anom)&& (mes3==mesm) && (dia3<diam))))
	  { diam = dia3;
		mesm = mes3;
		anom = ano3;
	}
	if (dia1=diam) && (mes1 == mesm) && (anos1=anom) System.out.print(nome1)
	if (dia2=diam) && (mes2 == mesm) && (anos2=anom) System.out.print(nome2)
	if (dia3=diam) && (mes3 == mesm) && (anos3=anom) System.out.print(nome3)
		
		}
}		
	  